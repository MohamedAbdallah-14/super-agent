# Artifact Templates

These templates are the canonical starting point for SuperAgent run outputs and committed examples.

Rules:

- include run ID, phase, role, loop number, and sources
- include approval status when the artifact is approval-gated
- cite evidence for non-trivial claims
- do not turn these templates into live run output or placeholder fake artifacts
